# Copy to `.env`:  cp .env.example .env
# The frontend must NEVER hard-code the backend URL - it reads it from here.

API_BASE_URL=http://localhost:8000

# Seconds to wait for an answer. A local LLM is slow on its first request
# because the model has to load into RAM.
REQUEST_TIMEOUT=180
